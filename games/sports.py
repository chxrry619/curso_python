class Sport:

    def __init__(self, name:str, players:init, league:str):
        self.name = name    
        if isinstance(players, int):
            self.players = players
        else:
            self.players    


            def to_json(self) -> dict:
                return{"name:self.name"}